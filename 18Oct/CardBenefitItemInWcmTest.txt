import static org.junit.jupiter.api.Assertions.*;

import org.junit.jupiter.api.Test;

public class CardBenefitItemInWcmTest {

    @Test
    public void testGettersAndSetters() {
        // Arrange
        CardBenefitItemInWcm item = new CardBenefitItemInWcm();
        String expectedId = "item123";
        boolean expectedPublish = true;
        String expectedProductionCode = "prodCode";
        String expectedHtml = "<div>Sample HTML</div>";

        // Act
        item.setId(expectedId);
        item.setPublish(expectedPublish);
        item.setProductionCode(expectedProductionCode);
        item.setHtml(expectedHtml);

        // Assert
        assertEquals(expectedId, item.getId());
        assertTrue(item.isPublish());
        assertEquals(expectedProductionCode, item.getProductionCode());
        assertEquals(expectedHtml, item.getHtml());
    }

    @Test
    public void testEquals_SameObject() {
        // Arrange
        CardBenefitItemInWcm item = new CardBenefitItemInWcm();
        
        // Act & Assert
        assertTrue(item.equals(item));
    }

    @Test
    public void testEquals_NullObject() {
        // Arrange
        CardBenefitItemInWcm item = new CardBenefitItemInWcm();
        
        // Act & Assert
        assertFalse(item.equals(null));
    }

    @Test
    public void testEquals_DifferentClass() {
        // Arrange
        CardBenefitItemInWcm item = new CardBenefitItemInWcm();
        
        // Act & Assert
        assertFalse(item.equals("String"));
    }

    @Test
    public void testEquals_SameValues() {
        // Arrange
        CardBenefitItemInWcm item1 = new CardBenefitItemInWcm();
        CardBenefitItemInWcm item2 = new CardBenefitItemInWcm();
        
        item1.setId("item123");
        item1.setPublish(true);
        item1.setProductionCode("prodCode");
        item1.setHtml("<div>Sample HTML</div");
        
        item2.setId("item123");
        item2.setPublish(true);
        item2.setProductionCode("prodCode");
        item2.setHtml("<div>Sample HTML</div");

        // Act & Assert
        assertTrue(item1.equals(item2));
    }

    @Test
    public void testEquals_DifferentValues() {
        // Arrange
        CardBenefitItemInWcm item1 = new CardBenefitItemInWcm();
        CardBenefitItemInWcm item2 = new CardBenefitItemInWcm();
        
        item1.setId("item123");
        item1.setPublish(true);
        item1.setProductionCode("prodCode");
        item1.setHtml("<div>Sample HTML</div>");
        
        item2.setId("item456");
        item2.setPublish(false);
        item2.setProductionCode("prodCode");
        item2.setHtml("<div>Different HTML</div>");

        // Act & Assert
        assertFalse(item1.equals(item2));
    }

    @Test
    public void testHashCode_SameValues() {
        // Arrange
        CardBenefitItemInWcm item1 = new CardBenefitItemInWcm();
        CardBenefitItemInWcm item2 = new CardBenefitItemInWcm();
        
        item1.setId("item123");
        item1.setPublish(true);
        item1.setProductionCode("prodCode");
        item1.setHtml("<div>Sample HTML</div>");
        
        item2.setId("item123");
        item2.setPublish(true);
        item2.setProductionCode("prodCode");
        item2.setHtml("<div>Sample HTML</div>");

        // Act & Assert
        assertEquals(item1.hashCode(), item2.hashCode());
    }

    @Test
    public void testHashCode_DifferentValues() {
        // Arrange
        CardBenefitItemInWcm item1 = new CardBenefitItemInWcm();
        CardBenefitItemInWcm item2 = new CardBenefitItemInWcm();
        
        item1.setId("item123");
        item1.setPublish(true);
        item1.setProductionCode("prodCode");
        item1.setHtml("<div>Sample HTML</div>");
        
        item2.setId("item456");
        item2.setPublish(false);
        item2.setProductionCode("prodCode");
        item2.setHtml("<div>Different HTML</div>");

        // Act & Assert
        assertNotEquals(item1.hashCode(), item2.hashCode());
    }
}
